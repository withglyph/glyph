{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "deps": { "dependsOn": ["^deps"] },
    "//#deps": { "dependsOn": ["^deps"] },

    "dev": {
      "cache": false,
      "dependsOn": ["codegen", "^build"],
      "persistent": true
    },
    "codegen": {},

    "build": { "dependsOn": ["codegen", "^build"] },

    "check": {}
  }
}
