{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": { "dependsOn": ["codegen", "^build"] },
    "codegen": {},
    "dev": { "dependsOn": ["codegen", "^build"], "persistent": true },
    "lint:svelte": {}
  }
}
