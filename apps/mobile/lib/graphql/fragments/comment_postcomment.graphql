fragment Comment_postComment on PostComment {
  id
  pinned
  visibility
  state
  purchased
  liked
  likedByPostUser
  masquerade {
    id
  }
  likeCount
  content
  createdAt

  profile {
    id
    name
  }
}
