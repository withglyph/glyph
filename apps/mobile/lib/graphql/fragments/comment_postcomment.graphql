fragment Comment_postComment on PostComment {
  id
  pinned
  visibility
  purchased
  liked
  likedByPostUser
  masquerade {
    id
  }
  likeCount
  content
  createdAt

  profile {
    id
    name
  }
}
