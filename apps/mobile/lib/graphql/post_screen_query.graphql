query PostScreen_Query($permalink: String!) {
  post(permalink: $permalink) {
    id
    permalink
    shortlink

    space {
      id
      slug
    }

    publishedRevision {
      id
      title
      subtitle
      content
    }

    bookmarkGroups {
      id
    }
  }
}