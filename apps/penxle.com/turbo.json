{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "codegen": {
      "dependsOn": ["codegen:prisma", "^build"],
      "outputs": [".svelte-kit/**", "$houdini/**"]
    },
    "codegen:prisma": { "cache": false }
  }
}
