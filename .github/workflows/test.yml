name: Test

on:
  pull_request:

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup workspace
        uses: ./actions/setup-workspace

      - name: Create lambda
        uses: ./actions/create-lambda
        with:
          entrypoint: ./functions/gh-app/src/index.ts
