{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "codegen": {
      "dotEnv": [".env"],
      "outputs": [".svelte-kit/**"]
    },
    "build": {
      "env": [
        "PRIVATE_*",
        "PUBLIC_*",
        "VERCEL_ANALYTICS_ID",
        "VERCEL_GIT_COMMIT_REF"
      ],
      "outputs": [".svelte-kit/**", ".vercel/**"]
    }
  }
}
