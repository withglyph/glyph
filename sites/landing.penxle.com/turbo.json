{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "codegen": {
      "dotEnv": [".env"],
      "outputs": [".svelte-kit/**"]
    },
    "build": {
      "dotEnv": [".env"],
      "outputs": [".svelte-kit/**", ".vercel/**"]
    }
  }
}
