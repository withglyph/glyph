{
  "name": "@penxle/monorepo",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "bootstrap": "pnpm install && husky install",
    "build:deps": "turbo run build --filter './packages/**'",
    "codegen": "turbo run codegen",
    "dev": "turbo run dev --filter './sites/**' --concurrency 255",
    "fix:all": "pnpm run '/^fix:.+$/'",
    "fix:eslint": "pnpm lint:eslint --fix",
    "fix:prettier": "pnpm lint:prettier --write",
    "lint:all": "pnpm run '/^lint:.+$/'",
    "lint:eslint": "eslint .",
    "lint:prettier": "prettier --check --ignore-unknown '**/*'",
    "lint:spellcheck": "cspell --no-progress --relative '**/*'",
    "lint:svelte": "turbo run lint:svelte",
    "lint:syncpack": "syncpack lint"
  },
  "devDependencies": {
    "@penxle/eslint-config": "workspace:^",
    "@penxle/prettier-config": "workspace:^",
    "cspell": "^7.0.0",
    "eslint": "^8.47.0",
    "gitmoji-cli": "^8.4.0",
    "husky": "^8.0.3",
    "lint-staged": "^14.0.0",
    "prettier": "^3.0.1",
    "syncpack": "^11.2.1",
    "turbo": "^1.10.12",
    "typescript": "^5.1.6"
  },
  "engines": {
    "node": "^18 || ^20",
    "pnpm": "^8.6.0"
  }
}
