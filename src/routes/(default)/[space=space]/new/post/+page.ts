import type { SpaceNewPostPage_QueryVariables } from './$houdini';

export const _SpaceNewPostPage_QueryVariables = (({ params }) => ({
  slug: params.space,
})) satisfies SpaceNewPostPage_QueryVariables;
