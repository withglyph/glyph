import type { SpacePublishPostPage_QueryVariables } from './$houdini';

export const _SpacePublishPostPage_QueryVariables = (({ params }) => ({
  slug: params.space,
})) satisfies SpacePublishPostPage_QueryVariables;
