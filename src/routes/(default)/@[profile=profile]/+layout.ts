import type { ProfileLayout_QueryVariables } from './$houdini';

export const _ProfileLayout_QueryVariables = (({ params }) => ({
  handle: params.profile,
})) satisfies ProfileLayout_QueryVariables;
